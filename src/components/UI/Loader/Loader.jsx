import React from 'react';
import cl from './Loader.module.css';



const Loader = () => {
        return (
                <div className={cl.loader} style={{alignItems: 'center'}}>
                        
                </div>
        );
};

export default Loader;